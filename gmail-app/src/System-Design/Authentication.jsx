function Authentication({ onLogin, onLogout }) {
  /** This will Render a login form with username and password fields and will call onLogin when user enters credential*/
  // will logout when user clicks logout button
}
