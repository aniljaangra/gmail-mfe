function ComposeEmail({ onSend }) {
  // it will render a form to compose a new email with recipient, subject, and message fields
  // will will call onSend with the email data when the user sends the email
}
